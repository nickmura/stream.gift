module twitch_donations::twitch_donations {
    use std::string::String;
    // TODO:
    // Table 
    // Send Money Transfer
    // event
    // struct
    //     /// List of todos. Can be managed by the owner and shared with others.
    //     public struct Donation has key, store {wwff
    //         id: UID,
    //         items: address<string>
    //     }

    //     /// Create a new todo list.
    //     public fun new(ctx: &mut TxContext): Donation {
    //         let list = Donation {
    //             id: object::new(ctx),
    //             items: vector[]
    //         };
    // ee
    //         (list)
    //     }

    //     /// Add a new todo item to the list.
    //     public fun add(list: &mut Donation, item: String) {
    //         list.items.push_back(item);
    //     }

    //     /// Remove a todo item from the list by index.
    //     public fun remove(list: &mut Donation, index: u64): String {
    //         list.items.remove(index)
    //     }

    //     /// Delete the list and the capability to manage it.
    //     public fun delete(list: Donation) {
    //         let Donation { id, items: _ } = list;
    //         id.delete();
    //     }

    //     /// Get the number of items in the list.
    //     public fun length(list: &Donation): u64 {
    //         list.items.length()
    //     }
}