//TODO: Record this data

export async function RecordDataCache(data:any[]) {
    let x = false
    for (let i = 0; i < data.length; i++) {
        // insert each row to a table, depending on eventType
        console.log(data[i].txDigest)
    }

}